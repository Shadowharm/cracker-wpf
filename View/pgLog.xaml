<Page x:Class="Cracker.pgLog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cracker"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Лог к " Loaded="Page_Loaded">

  <Page.Resources>
    <local:DateToStrConv x:Key="pgDTSC"/>
    <local:LogValueConverter x:Key="logValueConverter"/>
    <Style x:Key="ChatMessageStyle" TargetType="Border">
      <Setter Property="Background" Value="#2D2D30"/>
      <Setter Property="BorderBrush" Value="#3F3F46"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="8"/>
      <Setter Property="Padding" Value="12"/>
      <Setter Property="Margin" Value="8,4"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>
    <Style x:Key="TimeTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#A0A0A0"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>
    <Style x:Key="AuthorTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#4EC9B0"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>
    <Style x:Key="MessageTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#D4D4D4"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
  </Page.Resources>

  <Grid Background="#1E1E1E">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <ScrollViewer x:Name="scrollViewer" Grid.Row="0" VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled" Padding="10">
      <ItemsControl x:Name="itemsControl" ItemsSource="{Binding Logs}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Style="{StaticResource ChatMessageStyle}">
              <StackPanel>
                <TextBlock Text="{Binding time,Converter={StaticResource pgDTSC},ConverterParameter=datetime}" 
                          Style="{StaticResource TimeTextStyle}"/>
                <TextBlock Text="{Binding user}" Style="{StaticResource AuthorTextStyle}"/>
                <TextBlock Text="{Binding value,Converter={StaticResource logValueConverter}}" Style="{StaticResource MessageTextStyle}"/>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</Page>
